<%= form_with model:[book,book_comment], local: false do |f| %>
  <div class="form-group">
    <%= f.text_area :comment, class: "form-control border border-dark" %>
  </div>
  <div class="form-group">
    <%= f.submit "送信" %>
  </div>
<% end %>